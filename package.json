{
  "name": "tank-construction",
  "private": true,
  "version": "0.0.0",
  "description": "Tank Construction / Tank'Immo — plateforme SaaS BTP & promotion immobilière (Cameroun). Réf. CDC-TANK-2026-003.",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  },
  "engines": {
    "node": ">=20"
  }
}
