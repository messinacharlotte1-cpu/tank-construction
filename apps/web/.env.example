# Front web — variables PUBLIQUES uniquement (exposées au navigateur, préfixe VITE_).
# Ne JAMAIS mettre de secret ici (service_role key, mdp DB, clés MoMo/OM/SMS).
# Copier vers .env.local pour le dev, et renseigner côté Vercel (Project → Settings → Environment Variables).

VITE_SUPABASE_URL="https://<ref>.supabase.co"
VITE_SUPABASE_ANON_KEY="<clé anon / publishable — publique par design>"
